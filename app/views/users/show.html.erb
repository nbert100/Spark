<h1><%= @user.name %>'s Homepage </h1>

<ul>
<% @user.meetings.most_recent.each do |m| %>
    <p><%= m.appointment.strftime('%A, %b %d, %Y') %>
    <br>
    <%= link_to m.location, meeting_path(m) %> with <%= link_to m.suitor.name, suitor_path(m.suitor) %>
    <br></p>
    <% end %>
</ul>
